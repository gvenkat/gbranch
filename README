
* Gbranch

A utility to create remote branches, create a local one, track the changes on remote one and checkout the branch immediately
Just find it immensly useful utility to hangout in ~/bin directory
